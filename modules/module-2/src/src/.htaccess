# Block access to Composer files
<FilesMatch "^composer\.(json|lock)$">
    Require all denied
</FilesMatch>

# Prevent directory listing
Options -Indexes

# Tell Apache to use login.php as the default login page
DirectoryIndex login.php